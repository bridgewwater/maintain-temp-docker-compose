# fast use like

```sh
# make docker local folder
mkdir -p $HOME/actDocker
# get help
make help
# check config
make dockerCheck
# sync new
make dockerSync
```

- if change $HOME/actDocker, you must change scripts